import anvil.tables as tables
import anvil.tables.query as q
from anvil.tables import app_tables
"""Date/time helpers for user-local time handling.

Defines: _user_today(settings), _user_now(settings), _format_date_au(d),
_urgency_band(days_remaining).

Implementation pending - see IMPLEMENTATION_SPEC.md section 2 (server_code/_datetime.py).
"""

pass
