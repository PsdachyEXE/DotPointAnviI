"""Offline test suites for the DotPoint server modules.

Run from the repository root with:  python -m tests.run_all

These files are NEVER loaded by Anvil. Anvil only reads client_code/, server_code/,
theme/ and the top-level YAML config, so this directory is inert at runtime and
exists purely as verifiable evidence for SAT criterion 7.3.
"""
